# @package _global_
learner:
  hparams:
    model:
      regularization_loss_heads:
        jigsaw_prediction:
          head:
            _target_: sesemi.loss_heads.loss.JigsawPredictionLossHead
            data: unsupervised
            backbone: supervised_backbone